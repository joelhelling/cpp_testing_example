#include "Test_coveralls.h"

int Test_coveralls::get_val(int input)
{
  if (input == 1) {
    return val;
  }
  if (input == 2) {
    return val;
  }
  if (input == 3) {
    return val;
  }
  return val;
}
